From 9281c3b105f253f972cc19527336850121213736 Mon Sep 17 00:00:00 2001
From: James <aslan.jnn@gmail.com>
Date: Thu, 9 Jul 2026 14:30:42 +1000
Subject: Do not check for non-ascii character on minification

---
 build/lib/optimize.ts | 3 +--
 1 file changed, 1 insertion(+), 2 deletions(-)

diff --git a/build/lib/optimize.ts b/build/lib/optimize.ts
index 21a17ee1..794c2854 100644
--- a/build/lib/optimize.ts
+++ b/build/lib/optimize.ts
@@ -252,7 +252,7 @@ export function minifyTask(src: string, sourceMapBaseUrl?: string): (cb: any) =>
 
 					const contents = Buffer.from(jsOrCSSFile.contents);
 					const unicodeMatch = contents.toString().match(/[^\x00-\xFF]+/g);
-					if (unicodeMatch) {
+					if (false) {
 						cb(new Error(`Found non-ascii character ${unicodeMatch[0]} in the minified output of ${f.path}. Non-ASCII characters in the output can cause performance problems when loading. Please review if you have introduced a regular expression that esbuild is not automatically converting and convert it to using unicode escape sequences.`));
 					} else {
 						f.contents = contents;
@@ -281,4 +281,3 @@ function getBuildTarget() {
 	const tsconfigPath = path.join(REPO_ROOT_PATH, 'src', 'tsconfig.base.json');
 	return getTargetStringFromTsConfig(tsconfigPath);
 }
-
-- 
2.54.0 (Apple Git-156)

